This is my personal portfolio application built using React and Vite.

The project focuses on clean component structure, client-side routing with React Router, and data-driven rendering for projects. It demonstrates practical frontend architecture rather than heavy visual effects or overengineering.

The portfolio includes multiple routes (Home, Projects, Contact), reusable components, and asynchronous data fetching from public APIs.
